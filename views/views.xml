<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="smart_box.list">
      <field name="name">smart_box list</field>
      <field name="model">smart_box.smart_box</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="smart_box.action_window">
      <field name="name">smart_box window</field>
      <field name="res_model">smart_box.smart_box</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="smart_box.action_server">
      <field name="name">smart_box server</field>
      <field name="model_id" ref="model_smart_box_smart_box"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="smart_box" id="smart_box.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="smart_box.menu_1" parent="smart_box.menu_root"/>
    <menuitem name="Menu 2" id="smart_box.menu_2" parent="smart_box.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="smart_box.menu_1_list" parent="smart_box.menu_1"
              action="smart_box.action_window"/>
    <menuitem name="Server to list" id="smart_box" parent="smart_box.menu_2"
              action="smart_box.action_server"/>
    -->
  </data>
</odoo>